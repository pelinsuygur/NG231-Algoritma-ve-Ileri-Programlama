sayac=0
liste=[]
for sayi in range(100,1000,2):
    sayac+=1
    sayi=str(sayi)
    
    a=sayi[0]
    b=sayi[1]
    c=sayi[2]

    if int(a) != int(b) and int(a)!= int(c) and int(b)!=int(c):
        liste.append(sayi)

sayac=sayac-len(liste) 
print(sayac)
